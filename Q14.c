#include <stdio.h>

int main() {
    int day = 7; // Today is day 7

    if (day >= 1 && day <= 5) {
        printf("The shop is open.\n");
    } else {
        printf("The shop is closed.\n");
    }

    return 0;
}
