#include <stdio.h>

int main() {
    int attendance = 70;
    int marks = 85;

    if (attendance >= 75 && marks >= 70) {
        printf("The student qualifies.\n");
    } else {
        printf("The student does not qualify.\n");
    }

    return 0;
}
